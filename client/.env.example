# Client environment variables for ContextChat (Vite)
# Copy this file to .env and fill in the values

# Base URL of the backend API and Socket.IO server
# For local development when server runs on 5000:
VITE_API_URL=http://localhost:5000

# Production (Vercel frontend + Render backend):
# Set VITE_API_URL to your Render Web Service HTTPS URL. Example:
# VITE_API_URL=https://your-service.onrender.com

# Optional: For serving Vite over LAN with working HMR, you can set your LAN IP here and start dev as:
# VITE_DEV_HOST=192.168.1.10 npm run dev
# Then update VITE_API_URL above to use the same LAN IP and the backend port (e.g., 8989):
# VITE_API_URL=http://192.168.1.10:8989
