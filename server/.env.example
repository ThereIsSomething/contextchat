# Server environment variables for ContextChat
# Copy this file to .env and fill in the values

# HTTP port the Express/Socket.IO server listens on
PORT=5000

# Allowed client origin(s) for CORS and Socket.IO
# Comma-separated list is supported. For LAN development, include your machine's LAN IP origin.
# In production with Vercel frontend, set this to your Vercel URL(s).
# Examples:
# CLIENT_ORIGIN=http://localhost:5173
# CLIENT_ORIGIN=http://localhost:5173,http://192.168.1.10:5173
# CLIENT_ORIGIN=https://your-app.vercel.app
# CLIENT_ORIGIN=https://your-app.vercel.app,https://your-preview.vercel.app
CLIENT_ORIGIN=http://localhost:5173

# MongoDB connection
# Example for MongoDB Atlas:
# mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<db>?retryWrites=true&w=majority
MONGO_URI=
# Optional: override DB name (if not included in MONGO_URI)
MONGO_DB_NAME=contextchat

# JWT secret used to sign auth tokens
JWT_SECRET=super-secret-change-me
